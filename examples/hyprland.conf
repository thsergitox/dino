# dino — voice dictation TUI for Hyprland
# Run `dino setup` to generate and append the right block for your machine,
# or paste one of these manually and `hyprctl reload`.

# ─── Recommended: exec-once lifecycle ─────────────────────────────────────────
# dino starts with Hyprland and stays in a hidden special workspace; SUPER+Z
# toggles its visibility. Replace `kitty` with your terminal — for foot use
# `foot --app-id=dino dino tui`, for ghostty `ghostty --class=dino -e dino tui`.

exec-once = kitty --class dino --override hide_window_decorations=yes dino tui

windowrulev2 = workspace special:dino silent, class:^(dino)$
windowrulev2 = float, class:^(dino)$
windowrulev2 = size 60% 50%, class:^(dino)$
windowrulev2 = center 1, class:^(dino)$

bind = SUPER, Z, togglespecialworkspace, dino

# ─── Alternative: lazy-spawn lifecycle ────────────────────────────────────────
# bind = SUPER, Z, exec, ~/.config/dino/dino-toggle.sh
# (`dino setup` writes the toggle script if you pick this option.)

# ─── Legacy CLI (v0.1) ────────────────────────────────────────────────────────
# Push-to-talk style, no persistent window. Kept for users who don't want a TUI.
# bind  = SUPER, V, exec, dino start
# bindr = SUPER, V, exec, dino stop
